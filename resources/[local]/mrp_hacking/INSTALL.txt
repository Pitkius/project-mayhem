mrp_hacking – OS / tablet / visi apiplėšimai

1) ensure mrp_hacking (po qb-core, qb-inventory, qb-target, mrp_dispatch)

2) Aplėšimų tier'ai (visi implementuoti):
   • hack          — sekos minigame + animacija (laptop)
   • card          — rodyklių minigame + kortelės animacija
   • thermite      — hold SPACE minigame + termito animacija
   • drill         — timing minigame + gręžtuvo animacija/prop
   • loot          — mash SPACE minigame + grab animacija
   • ATM drill     — timing (4 raundai) + gręžimas
   • ATM chain     — rodyklių minigame + mechaniko animacija
   • ATM crack     — hack minigame + animacija
   • store        — 24/7 kasa (BlackOS + basic tablet)
   • bank_fleeca  — Fleeca (GhostOS + advanced tablet + drill)
   • bank_main    — Pacific (CipherOS + kortelė A + termite + drill)
   • casino       — Kazino serverinė (CipherOS + military tablet)
   • vault        — Federal vault (FederalOS + kortelė B + termite + drill)

3) Vietos: qb-target zona prie koordinačių (config_robberies.lua).
   Tablet Network skiltis rodo fazės ir vietų skaičių.

4) TEST pardavėjas: ~ -328, -134 (DebugHeistVendor — $1 itemai; production: enabled = false)

5) Black market: 707, -967 (normalios kainos + OS payload ant USB)

6) Tipinis startas:
   a) basic_tablet + flashdrive SU PAYLOAD (test NPC → „Flashdrive OS / exploit“, ne tuščias iš shop)
   b) Naudok flashdrive arba planšetę → Storage → Install payload
   c) Atlik hack minigame → fazės (drill/termite/kortelė pagal tier)

7) Restart po config pakeitimų:
   ensure qb-core
   ensure mrp_hacking
