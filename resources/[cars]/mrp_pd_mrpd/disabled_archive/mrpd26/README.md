# mrpd26 — archived duplicate of mrpd11

Stream files were byte-identical to `mrpd11`. The buggy floating red DrawLight
blocks came from `mrpd11` carcols (`light=true`). Slot **mrpd11** now uses the
safe `mrpd26` carcols kit (`light=false`, balanced R/B intensity).

Do not restore this folder into live `data/`/`stream/` unless you give it a
unique siren kit id (conflict with mrpd11 kit 1229).
