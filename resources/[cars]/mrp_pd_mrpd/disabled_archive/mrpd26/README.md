# mrpd26 — archived duplicate of mrpd11

Stream files were byte-identical to `mrpd11`.

`mrpd11` now uses its **original** siren kit id **4928** (balanced R/B, `light=false`) —
not the mrpd26 kit (1229), which mapped poorly and looked red-only after the swap.

Do not restore this folder into live `data/`/`stream/` unless you give it a
unique siren kit id (1229 is free again only while mrpd11 stays on 4928).