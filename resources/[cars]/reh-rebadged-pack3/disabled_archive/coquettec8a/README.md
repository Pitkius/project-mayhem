# Disabled: coquettec8a

Removed from live stream/meta and dealership (duplicate Invetero Coquette C8; `coquettec8` remains).

To re-enable: move `data/coquettec8a` → `../../data/` and stream files → `../../stream/`, then restore QB/dealership/perf entries.
