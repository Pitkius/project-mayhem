# mrpd21 — was disabled (corrupt YFT), restored

Corrupt `mrpd21.yft` / `mrpd21_hi.yft` (UTF-8 `EF BF BD` after RSC7) caused
client crashes on build 3751. Live copies restored from git commit `f41422c3`
(clean RSC7 headers). Archive kept for reference only — do not re-stream these
corrupt YFTs.
